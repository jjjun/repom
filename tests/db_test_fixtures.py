"""
テスト用データベースフィクスチャ

⚠️ 重要: このファイルは後方互換性のために残していますが、
実際のフィクスチャ定義は tests/conftest.py に移動しました。

pytest はプロジェクトルートの conftest.py からフィクスチャを
自動的に検出します。

フィクスチャの詳細:
- db_engine: session スコープ（全テストで1回だけDB作成）
- db_test: function スコープ（各テストで独立したトランザクション）

トランザクションロールバック方式:
- 高速化: DB再作成不要、ロールバックのみ
- 完全な分離: 各テストが独立したトランザクション内
- クリーンな状態: 自動ロールバックで確実にリセット

パフォーマンス改善:
- 旧方式: 153テスト × DB再作成(0.15s) = 約23秒
- 新方式: DB作成1回(0.5s) + ロールバック(0.001s × 153) = 約0.65秒
- 改善効果: 約35倍高速化（DB操作部分のみ）
"""

# このファイルは後方互換性のために残していますが、
# 実際のフィクスチャは tests/conftest.py で定義されています。
# 新しいテストを書く場合は、conftest.py を参照してください。
